import React from "react";
import "@testing-library/jest-dom/extend-expect";
import { render } from "@testing-library/react";
import ErrorBoundaryContextProvider from "./ErrorBoundaryContextProvider";

test("dummy", () => {});

// describe("ErrorBoundaryContextProvider", (): void => {
//   test("expect ErrorBoundaryContextProvider to render", (): void => {
//     const { getByText } = render(
//       <ErrorBoundaryContextProvider>
//         <div>ErrorBoundaryContextProvider</div>
//       </ErrorBoundaryContextProvider>
//     );

//     expect(getByText("ErrorBoundaryContextProvider")).toBeInTheDocument();
//   });
// });
